server.port=8081
server.error.include-message=always
spring.profiles.active=local

#url properties
url.base=http://localhost:8081/

#keycloak
keycloak.realm=core-realm
keycloak.resource=core-keycloak
keycloak.ssl-required=external
keycloak.use-resource-role-mappings=true
keycloak.bearer-only=true
swagger.keycloak.auth-server-url=http://localhost:8080/auth
keycloak.auth-server-url=http://localhost:8080/auth
keycloak.credentials.secret=y5cbfJ7GdntkPTBgWsCpgXoj2SxECY9W

#postgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/core?useSSL=false&serverTimezone=UTC
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.event.merge.entity_copy_observer=allow
